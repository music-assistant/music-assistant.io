---
import { PAGE_TITLE_ID } from '../constants';
import Default from '@astrojs/starlight/components/PageTitle.astro';

const isBlogPost = Astro.url.pathname.startsWith('/blog/');
---

{isBlogPost ? (
	<Default {...Astro.props} />
) : (
	<div id={PAGE_TITLE_ID}></div>
)}